import BezorgdatumPage from '../page'

/** Catches /bezorgdatum/ORDER%2Femail%40domain.com (single segment = order/email or order#email) */
export default function BezorgdatumSlugPage() {
  return <BezorgdatumPage />
}
